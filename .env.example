# R36S Android Build Configuration
# Copy this file to .env and customize as needed

# Number of parallel jobs for repo sync (default: 4)
# Increase for faster downloads if you have a good connection
REPO_SYNC_JOBS=4

# Build target variant (default: lineage_r36s-userdebug)
BUILD_TARGET=lineage_r36s-userdebug

# Local manifests branch (default: main)
# Use different branches to build with different configurations or patches
# Examples:
#   - main: Standard build
#   - analog-stick-mouse: Build with analog stick mouse support
LOCAL_MANIFESTS_BRANCH=main
